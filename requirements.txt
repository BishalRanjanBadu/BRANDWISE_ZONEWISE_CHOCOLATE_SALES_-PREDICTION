# SERVING pins. These MUST match the library versions recorded in the promoted
# artifact's manifest.json, which came from the Phase-1 training environment.
# A delta here makes the unpickle silently wrong; registry.load_current()
# fails readiness rather than serving it.
# Every version verified published on PyPI with a cp313 aarch64 wheel
# (the t4g free-tier node is ARM64) - nothing compiles from source.
lightgbm==4.6.0
scikit-learn==1.6.1
numpy==2.1.3
pandas==2.2.3
scipy==1.15.1
boto3==1.43.87
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
streamlit==1.63.0
requests==2.34.2
